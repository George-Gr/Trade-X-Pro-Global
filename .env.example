# Supabase Configuration
# These variables are required for the application to function
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Sentry Configuration (Optional - for error tracking in production)
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/your-project-id

# Build Environment
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. Get your Supabase URL and publishable key from:
#    - Supabase Dashboard > Project Settings > API
# 4. Get your Sentry DSN from:
#    - Sentry Dashboard > Settings > Projects > Client Keys (DSN)
#
# IMPORTANT:
# - Never commit .env.local to version control
# - .env.local is already in .gitignore
# - Keep sensitive values secure and never share them publicly
