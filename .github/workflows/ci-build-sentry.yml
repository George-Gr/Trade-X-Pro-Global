name: CI Build (with optional Sentry DSN)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout with full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build application
        env:
          VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN }}
          VITE_APP_VERSION: ${{ github.sha }}
        run: npm run build

      - name: Get release version
        id: get_version
        run: |
          VERSION=$(node -e "try{const p=require('./package.json'); console.log(p.version || process.env.GITHUB_SHA);}catch(e){console.log(process.env.GITHUB_SHA);}")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 Release version: $VERSION"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: site-dist
          path: dist

      - name: Notify Sentry configuration
        if: secrets.VITE_SENTRY_DSN != ''
        run: echo "✓ Sentry DSN configured - error tracking enabled"

      - name: Install Sentry CLI
        if: secrets.SENTRY_AUTH_TOKEN != '' && secrets.SENTRY_ORG != '' && secrets.SENTRY_PROJECT != ''
        run: |
          echo "📥 Installing Sentry CLI..."
          curl -sL https://sentry.io/get-cli/ | bash
          sentry-cli --version

      - name: Create Sentry release
        if: secrets.SENTRY_AUTH_TOKEN != '' && secrets.SENTRY_ORG != '' && secrets.SENTRY_PROJECT != ''
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
        run: |
          VERSION="${{ steps.get_version.outputs.version }}"
          echo "🚀 Creating Sentry release: $VERSION"
          
          # Create new release
          sentry-cli releases new "$VERSION"
          
          # Associate commits (ignore missing to prevent errors)
          sentry-cli releases set-commits "$VERSION" --auto --ignore-missing || echo "⚠️  Could not associate commits, continuing..."
          
          # Finalize release
          sentry-cli releases finalize "$VERSION"
          
          echo "✓ Sentry release created successfully"

      - name: Upload source maps to Sentry
        if: secrets.SENTRY_AUTH_TOKEN != '' && secrets.SENTRY_ORG != '' && secrets.SENTRY_PROJECT != ''
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
        run: |
          VERSION="${{ steps.get_version.outputs.version }}"
          echo "📤 Uploading source maps to Sentry..."
          
          sentry-cli releases files "$VERSION" upload-sourcemaps ./dist --url-prefix "~/" --rewrite
          
          echo "✓ Source maps uploaded successfully"

      - name: Deploy Sentry release
        if: secrets.SENTRY_AUTH_TOKEN != '' && secrets.SENTRY_ORG != '' && secrets.SENTRY_PROJECT != ''
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
        run: |
          VERSION="${{ steps.get_version.outputs.version }}"
          echo "🌐 Deploying Sentry release to production..."
          
          sentry-cli releases deploys "$VERSION" new -e production
          
          echo "✓ Sentry release deployed to production"

      - name: Workflow complete
        run: |
          echo "================================================"
          echo "✓ CI Build completed successfully"
          echo "================================================"
