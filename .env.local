# .env.local - Local development environment variables (REDACTED)
# SENSITIVE VALUES HAVE BEEN REMOVED FROM THIS COPY AND REPLACED WITH PLACEHOLDERS
# DO NOT COMMIT SENSITIVE VALUES TO THE REPOSITORY

# Supabase Configuration
VITE_SUPABASE_URL=https://oaegicsinxhpilsihjxv.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<REDACTED_PUBLISHABLE_KEY>

# API Configuration
VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000

# Environment Mode
VITE_ENV=development

# Sentry Error Tracking (Optional)
# VITE_SENTRY_DSN=<REDACTED_SENTRY_DSN>

# Documentation URL
VITE_DOCS_URL=https://docs.tradexpro.com/api

# Feature Flags (Optional)
# VITE_ENABLE_ADVANCED_CHARTS=false
# VITE_ENABLE_COPY_TRADING=false
# VITE_ENABLE_API_ACCESS=false

# Context7 MCP Server Configuration
CLIENT_IP_ENCRYPTION_KEY=<REDACTED_CLIENT_IP_ENCRYPTION_KEY>

# ---- SECURITY NOTICE ----
# This file formerly contained sensitive credentials that were removed on %DATE%.
# Next manual steps (run locally with appropriate privileges):
# 1) Remove the committed file from Git history for the current branch:
#    git rm --cached .env.local
#    git commit -m "chore(security): remove .env.local containing secrets"
#    git push
# 2) Rotate any affected keys immediately in the provider dashboards (Supabase, Sentry, Finnhub, etc.)
# 3) Run a repository secret scan and history scrub if required (e.g., trufflehog, git-filter-repo)
# 4) Add new values to your CI secrets manager (GitHub Secrets or Vault) and update deployments
# 5) Verify there are no remaining secrets in the repo and monitor for suspicious access

# If you need assistance performing rotations and history rewrites, contact the security team.
